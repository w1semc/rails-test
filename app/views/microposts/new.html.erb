<% provide(:title, 'Create Post') %>
<h1>Create Post</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@micropost) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :Title %>
      <%= f.text_field :zagolovok, placeholder: "Compose new title..." %>

      <%= f.label :content %>
      <%= f.text_field :content, placeholder: "Compose new post..." %>

      <%= f.submit "Post", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
